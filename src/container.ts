// Import the console scss here. This should be the only css import here.
// Use sass to import sub-modules/modules for variables and extra helpers.
//import './scss/container.scss';


import "./ts/AppContainer";

import UIkit from 'uikit';
import Icons from 'uikit/dist/js/uikit-icons';

// loads the Icon plugin
UIkit.use(Icons);
